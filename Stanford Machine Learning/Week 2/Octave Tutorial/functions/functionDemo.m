function y = functionDemo(x)
  y = x^2;
end;

% NB!! only one function per file, with the same name
% the structure is:
% declaration that this is a function, i.e

% function([return values]) = functionName([arguments])
% statements
% end;